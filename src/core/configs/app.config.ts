import { IAppConfig } from '../interfaces';

export const AppConfig: IAppConfig = {
    server: {
        port: 47808,
        sequence: {
            thread: 1,
            delay: 20,
        }
    }
};
